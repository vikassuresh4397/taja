// const express = require('express');
// const blogController = require('../controllers/blogController');
// const authentication = require('../middleware/auth.middleware');
// const Postrouter = express.Router();


// Postrouter.get('/blogs', authentication, blogController.getBlogs);

// Postrouter.get('/blogs/title', authentication, blogController.getBlogsByTitle);

// Postrouter.get('/blogs/category', authentication, blogController.getBlogsByCategory);

// Postrouter.get('/blogs/sort', authentication, blogController.getSortedBlogs);

// Postrouter.post('/blogs', authentication, blogController.createBlog);

// Postrouter.put('/blogs/:id', authentication, blogController.updateBlog);

// Postrouter.delete('/blogs/:id', authentication, blogController.deleteBlog);

// Postrouter.put('/blogs/:id/like', authentication, blogController.likeBlog);

// Postrouter.put('/blogs/:id/comment', authentication, blogController.commentOnBlog);

// module.exports = {Postrouter};